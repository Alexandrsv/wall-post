{
  "name": "@wall-post/prisma",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "dev": "tsc --watch --preserveWatchOutput",
    "build": "tsc",
    "ts:check": "tsc --noEmit",
    "lint": "echo 'ESLint not (fully) configured for prisma package, skipping...'",
    "postprisma:generate": "yarn build"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prisma": "^5.10.0",
    "typescript": "^5.3.3"
  },
  "files": [
    "dist",
    "src",
    "prisma/schema.prisma"
  ]
}
